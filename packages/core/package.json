{
    "name": "@burndata/core",
    "version": "1.0.0",
    "private": true,
    "description": "Core types, interfaces, and engine for BurnData",
    "main": "src/index.ts",
    "types": "src/index.ts",
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "clean": "rimraf dist",
        "typecheck": "tsc --noEmit",
        "lint": "eslint src --ext .ts --max-warnings 0"
    },
    "devDependencies": {
        "@types/node": "^20.19.32",
        "rimraf": "^5.0.10",
        "typescript": "^5.9.3"
    }
}
