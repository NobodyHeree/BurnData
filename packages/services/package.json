{
    "name": "@burndata/services",
    "version": "1.0.0",
    "private": true,
    "description": "Platform services for BurnData (Discord, Twitter, Reddit, etc.)",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "clean": "rimraf dist",
        "test": "vitest run",
        "lint": "eslint src --ext .ts --max-warnings 0",
        "typecheck": "tsc --noEmit"
    },
    "dependencies": {
        "axios": "^1.13.4"
    },
    "devDependencies": {
        "@types/node": "^20.19.32",
        "rimraf": "^5.0.10",
        "typescript": "^5.9.3"
    }
}